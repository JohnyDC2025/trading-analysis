name: EUA — Análise Noite

on:
  schedule:
    # 22:10h Lisboa (UTC+1 verão | UTC+0 inverno)
    # Verão  (Mar–Out): 21:10 UTC → 22:10 Lisboa  ← activo
    # Inverno (Out–Mar): mudar para '10 22 * * 1-5'
    # Mercado EUA fecha às 21h Lisboa (verão) / 22h (inverno)
    - cron: '10 21 * * 1-5'
  workflow_dispatch:

jobs:
  us-scan:
    runs-on: ubuntu-latest
    env:
      MARKET: us

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Data Lisboa
        id: date
        run: echo "today=$(TZ='Europe/Lisbon' date +'%d/%m/%Y')" >> $GITHUB_OUTPUT

      - name: Correr screener americano
        run: node screener.mjs

      - name: Enviar email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.GMAIL_USERNAME }}
          password: ${{ secrets.GMAIL_APP_PASSWORD }}
          subject: "🇺🇸 Top 15 EUA — ${{ steps.date.outputs.today }}"
          to: joaommdc@gmail.com
          from: "Stock Analysis <${{ secrets.GMAIL_USERNAME }}>"
          html_body: file://report.html
